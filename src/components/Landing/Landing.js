import React from 'react';

// import * as ROUTES from '../../constants/Routes';

const Landing = () => {
    return(
        <div>
            <h2>Welcome to Project Ascendance!</h2>
            <br/>
            <h3>This is where we'll put some general info and or stuff like a mission statement etc.</h3>
            <p>IDEALLY also a way to search for volunteer opportunites for those not signed up, then if they find something they are interested in, they can then be prompted to create an account</p>
        </div>
    )
};

export default Landing;